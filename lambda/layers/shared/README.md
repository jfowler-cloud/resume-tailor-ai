# Lambda Layer

This directory contains shared utilities for Lambda functions.

## Structure

```
lambda/layers/shared/
└── python/
    └── shared_utils.py
```

The `python/` directory is required for Lambda layers to work correctly.
